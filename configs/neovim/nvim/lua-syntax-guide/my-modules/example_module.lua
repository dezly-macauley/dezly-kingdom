-- ABOUT: Creating Modules

-- A module is simply a reusable block of Lua code 

-- This creates a Lua table called `M`
-- `M` is just a naming convention that means Module

-- NOTE: Lua does not have a specific syntax that is for turning a file into 
local M = {}
